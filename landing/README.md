# jobpull Landing

Marketing site for the `jobpull` project.

## Commands

```bash
npm run dev -w landing
npm run build -w landing
npm run lint -w landing
```

## Notes

- Source lives in `landing/src/`
- The page should describe the product as it exists in this repo
- Keep external links pointed at the main project docs or customization guide
