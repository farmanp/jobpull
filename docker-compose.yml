services:
  jobpull:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env.docker
    environment:
      PORT: 8787
      DB_PATH: /data/jobpull.sqlite
    ports:
      - "8787:8787"
    volumes:
      - ./data:/data
    restart: unless-stopped
